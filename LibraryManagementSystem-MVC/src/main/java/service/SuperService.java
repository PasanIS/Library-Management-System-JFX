package service;

public interface SuperService {
    // This interface can be used to define common methods for all services
    // For example, you might want to add methods like validate, log, etc.
    // Currently, it serves as a marker interface for service types.
}
